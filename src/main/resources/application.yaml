spring:
  application:
    name: death

server:
  forward-headers-strategy: framework

app:
  cors:
    allowed-origins:
      - http://localhost:3000
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
